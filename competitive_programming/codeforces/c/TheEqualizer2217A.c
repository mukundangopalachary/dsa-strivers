#include <stdio.h>

int main() {

  int t;
  scanf("%d", &t);

  for (int i = 0; i < t; i++) {

    int n, k;
    scanf("%d %d", &n, &k);

    int nums[n];
    for (int j = 0; j < n; j++) {
      scanf("%d", nums[i]);
    }
  }
}
